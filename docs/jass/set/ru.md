# `//set` — Директива настройки файла

Директива `//set` позволяет настраивать параметры языкового сервера для
конкретного файла.  Она должна находиться **в самом начале** файла,
рядом с директивами `//import`, до любых языковых конструкций.

## Синтаксис

```jass
//set <ключ> <значение>
```

* Токен `//set` должен начинаться с **колонки 0** (без ведущих пробелов).
* `<ключ>` — имя настройки (пробелы в ключе не допускаются).
* `<значение>` — всё после ключа до конца строки (пробелы обрезаются).

## Пример

```jass
//import common/natives.j
//set ref-tip 1

globals
    integer count = 0
endglobals
```

## Доступные настройки

| Ключ | Значения | Описание |
|------|----------|----------|
| `ref-tip` | `1` / `0` | Показать / скрыть подсказки с ID ссылок рядом с каждым идентификатором. Полезно для отладки разрешения символов и рефакторинга переименований. |
| `build-jass` | `<путь>` | Путь для сборки JASS. Объединяет всё дерево импортов в один `.j` файл: типы → native → globals → функции (в топологическом порядке) → `main`. Если путь — директория, добавляется `war3map.j`. |
| `build-as` | `<путь>` | Путь для сборки AngelScript. Та же логика слияния, но с синтаксисом `.as`. Конфликты с зарезервированными словами разрешаются добавлением числового суффикса. |

## Поведение

* Настройки действуют только в пределах одного файла — они не
  распространяются через `//import`.
* Нераспознанные ключи принимаются без ошибок (для прямой совместимости).
* Отсутствующее значение вызывает предупреждение (warning).


